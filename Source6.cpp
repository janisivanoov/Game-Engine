#include "stdafx.h"
#include "Header2.h"
int main(){
    Engine engine; // Declaring an instance of the Engine class
    engine.start(); // Calling the start function
    return 0;  // Stop the program the program when the engine is stopped
}